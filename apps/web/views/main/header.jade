div#header(class='navbar navbar-default navbar-static-top')
  .container
    .navbar-header
      button.navbar-toggle(type='button', data-toggle='collapse', data-target='.navbar-collapse')
        span.sr-only Nav Toggle
        span.icon-bar
        span.icon-bar
        span.icon-bar
      a.navbar-brand(href='/') Mike and Amelia
    .navbar-collapse.collapse
      ul.nav.navbar-nav.navbar-right
        {{#if user}}
        li.dropdown
          a.dropdown-toggle(href='#', data-toggle='dropdown') {{user.name}}&nbsp;
            b.caret
          ul.dropdown-menu
            li
              a(href='/guests/me') Edit RSVP
            li
              a(href='/guests/logout') Logout
        {{else}}
        li
          a(href='/guests/login') Login
        li
          a(href='/guests/register') Register
        {{/if}}